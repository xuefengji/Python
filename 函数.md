# 函数

1、函数定义

def func(argments):

​	pass

2、函数参数

+ 形参
+ 默认参数：必须在形参之后
  + 默认参数必须指向不变对象
  + 使用参数名传递参数
+ 可变参数：*args
  + 必须放在所以参数和默认参数的后面
  + 如果传入的是 list 会将整个 list 当作一个参数传入
+ 关键字参数：**kw
+ 命名关键字

3、递归函数：一个函数在内部调用自身本身，就是递归函数